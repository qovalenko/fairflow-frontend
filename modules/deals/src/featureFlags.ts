/**
 * Фиче-флаги модуля «Сделки».
 *
 * TODO-187: импорт сделок был полным моком — файл не читался, ни одного запроса
 * не уходило, а на шаге «Результат» показывались захардкоженные «создано 10,
 * обновлено 2». Пользователь видел успешный импорт, которого не было.
 *
 * Серверной ручки импорта сделок не существует: в gateway есть только
 * `POST /v1/contacts/import` (crm-bff.controller.ts) и `POST /v1/companies/import`
 * (v1-data-bff.controller.ts). Пока её нет, точки входа в импорт скрыты, а маршрут
 * `/deals/import` честно сообщает о недоступности.
 *
 * Чтобы включить: реализовать в gateway `POST /v1/deals/import` (право
 * `deals:import`, тот же контракт ответа `ImportResult`, что у контактов),
 * реализовать мастер по образцу `host/src/views/crm/Import/ImportWizard.tsx`
 * (реальный парсинг CSV + маппинг колонок) и поставить здесь `true`.
 */
export const DEALS_IMPORT_ENABLED = false
